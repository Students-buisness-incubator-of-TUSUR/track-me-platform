plugins {
    id 'io.freefair.lombok' version '8.14' apply false
}

subprojects {
    // Версия по умолчанию задаётся в gradle.properties (X.Y.Z-SNAPSHOT),
    // CI переопределяет её при релизе: ./gradlew publish -Pversion=X.Y.Z
    apply plugin: 'maven-publish'

    publishing {
        repositories {
            maven {
                name = 'GitHubPackages'
                url = uri('https://maven.pkg.github.com/Students-buisness-incubator-of-TUSUR/track-me-platform')
                credentials {
                    username = project.findProperty('gpr.user') ?: System.getenv('GITHUB_ACTOR')
                    password = project.findProperty('gpr.key') ?: System.getenv('GITHUB_TOKEN')
                }
            }
        }
    }

    plugins.withId('java') {
        apply plugin: 'checkstyle'

        checkstyle {
            toolVersion = '10.12.5'
            configFile = rootProject.file('config/checkstyle/checkstyle.xml')
            ignoreFailures = true  // Set to false to fail build on violations
            maxWarnings = Integer.MAX_VALUE  // Set to 0 to fail on any warnings
        }

        tasks.withType(Checkstyle).configureEach {
            reports {
                xml.required = true
                html.required = true
            }
        }
    }
}
